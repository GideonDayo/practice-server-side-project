{
  "name": "server-side-project",
  "version": "1.0.0",
  "description": "Fetches images of dogs",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "node start",
    "dev": "concurrently \"nodemon index.js\" \"npm --prefix my-react-app run dev\"",
    "start:server": "node index.js",
    "build-and-start": "npm --prefix my-react-app run build && NODE_ENV=production node index.js"
  },
  "keywords": [],
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "redis": "^6.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.0"
  }
}
